var mongoose = require('mongoose');

var offerRequestsSchema = new mongoose.Schema({
    offerid : String,
    username : String,
    date : Date,
    comment : String,
    status : String
});

module.exports = mongoose.model('OfferRequests', offerRequestsSchema);